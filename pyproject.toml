[project]
name = "genai_flask_app"
version = "0.1.0"
description = "ibm task"
readme = "README.md"
authors = [
    { name = "Bruno Carpio", email = "bcarpio181@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
genai = "genai_flask_app:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
